# connector-runner-mail-source

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build connector-runner-mail-source` to build the library.

## Running unit tests

Run `nx test connector-runner-mail-source` to execute the unit tests via [Jest](https://jestjs.io).
