# connector-runner-mail-sink

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build connector-runner-mail-sink` to build the library.

## Running unit tests

Run `nx test connector-runner-mail-sink` to execute the unit tests via [Jest](https://jestjs.io).
