# connector-releases

## Install
To install the connector releases, run the following command to download and execute the installation script:

```bash
curl -o- https://raw.githubusercontent.com/xip-online-applications/connector-releases/master/install.sh | bash
```
